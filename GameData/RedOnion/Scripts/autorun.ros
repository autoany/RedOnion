if ship == null
  return
if ship.parts.count == 1
  return //kerbal or ground experiment
//note: direct run would block scripts from repl
system.main def => run ship.prelaunch ? "launch.ros" : "control.ros"
